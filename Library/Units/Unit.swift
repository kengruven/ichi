protocol Unit {
    // WRITEME: ??
}

struct BaseUnit: Unit {
    // WRITEME

    // KLUGE: just for testing:
    init(string name: String) {
        // WRITEME!  store it or something.
    }
}

struct DerivedUnit: Unit {
    // WRITEME
}
